# Camapro Scope control protocol — G1 contract fixtures

Generated by scripts/gen_fixtures.py. Each case declares its expected validation
outcome; the test suite fails if observed behavior diverges from expectation.

- valid: must pass schema AND semantic envelope validation.
- invalid: must be rejected (schema or semantics), with the named reason class.
- semantic: schema-valid but rejected/accepted only by semantic rules.
