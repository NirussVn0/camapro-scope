# Camapro Scope Project Replan Implementation Plan

> **For Hermes:** Use the repository-local `skills/verified-slice/SKILL.md` workflow with bounded workers and independent major-gate review. No external skill name is assumed installed.

**Goal:** Convert the design-only repository into one coherent, evidence-gated implementation baseline with portable agent workflows.

**Architecture:** Retain Android Camera2 and a Rust/GStreamer desktop core, with Tauri/React limited to controls. Resolve media feasibility, native preview and trust before full implementation; one command/session authority owns lifecycle.

**Tech Stack:** Kotlin/Compose/Camera2, Rust/GStreamer, Tauri 2/React/TypeScript, versioned JSON, MJPEG bootstrap.

---

## Scope of this document

This is the record of the documentation replan, not a second application roadmap. The only active execution plan is [docs/ROADMAP.md](../../docs/ROADMAP.md). No application code is implemented by this replan.

## Tasks

1. Inspect all authored baseline files and Git state; preserve pre-existing untracked content before rewriting. Record path-cited findings in `docs/ASSESSMENT.md`.
2. Reconcile `README.md`, `docs/ARCHITECTURE.md`, `docs/PROTOCOL.md`, `docs/PLATFORMS.md`, `docs/DEVELOPMENT.md` and `prompts/IMPLEMENT.md`. Label current vs future behavior, native-preview uncertainty and draft schema status honestly.
3. Create `docs/DECISIONS.md`; retain existing stack and put new security/resume/support policy behind explicit approval/evidence gates.
4. Rewrite `docs/ROADMAP.md` with G0 risk spikes, G1 contract/bootstrap and G2 safe first pixel before OS output/controls/release gates. Give exact proposed source/test/evidence paths, dependencies and stop conditions.
5. Create `AGENTS.md` and `skills/{scope-planning,protocol-session,media-platform,verified-slice}/SKILL.md`; route by task class, avoid user-global installation and duplicated agent canons.
6. Validate local Markdown links, skill frontmatter/routing, JSON syntax and preserved schema hash; inspect changes against baseline because ordinary Git diff ignores untracked files. Get independent semantic review and fix findings before final checks.

## Validation boundaries

Document consistency and JSON syntax can be checked now; future runtime/protocol semantic gates cannot pass before their implementations exist. No fabricated build/device outputs. Do not report the current draft schema as production-safe merely because its JSON parses.

## Risks and decisions

The original files are untracked; preserve their ownership and do not stage/commit automatically. No reference phone or implementation SDK floor is selected yet. See the decision ledger for required approvals; see the roadmap for the next executable task.
